<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name = "android:windowActionBar">false</item>
        <item name = "android:windowNoTitle">true</item>
    </style>

    <style name="SwitchProfile" parent="Theme.AppCompat.Light">
        <!-- активное состояние -->
        <item name="colorControlActivated">#979797</item>
        <!-- цвет дорожки для активного состояния -->
        <item name="trackTint">#f3c647</item>

        <!-- #c5c5c5 -->

        <!-- неактивное состояние -->
        <!--<item name="colorSwitchThumbNormal"></item>-->
        <!-- цвет дорожки для неактивного состояния -->
        <!--<item name="android:colorForeground">@android:color/darker_gray</item>-->

    </style>

    <style name="EdittextDonate" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorDonateEdittext</item>
        <item name="colorControlActivated">@color/colorDonateEdittext</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
